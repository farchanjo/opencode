# Prose feature scenarios (not executed by speckit verify)
#
# Executable coverage lives in package unit/integration tests.
# Spec Acceptance Scenarios remain the scored scenarioCoverage source.
#
Feature: Wire Per Subagent Hierarchy Delegation Into The Task Spawn
  As a [placeholder actor]
  I want to [placeholder action]
  So that [placeholder benefit]

  Scenario: [placeholder scenario description]
    Given [a precondition is in place]
    When  [the actor performs an action]
    Then  [an observable outcome should occur]
