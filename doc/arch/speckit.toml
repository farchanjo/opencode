# speckit project configuration (ADR-0029): the merged control-plane
# source of truth. Lives under doc/arch, so it is committed and in guard scope.

[git]
email = "farchanjo@gmail.com"
# Recognized upstream identities (ADR-0025/0026 foreign exemption): commits
# authored under these are upstream history synced from anomalyco/opencode and
# are exempt from the commit-discipline gate; own commits stay enforced.
foreignEmails = [
  "*@users.noreply.github.com",
  "*@sst.dev",
  "noreply@github.com",
]

# The ACTIVE spec scope is the UNION of the static globs below (operator-managed)
# and derived globs computed at startup: doc/arch/sdd/NNN-slug/** for the active
# feature (its branch), doc/.specify/**, and doc/arch/** (always). Do NOT list
# the derived globs here.
[guard]
mode = "enforce"               # "enforce" (default) | "advisory"
overrideFlag = "--allow-out-of-spec"
specScopeGlobs = [
  # Feature 007 Phase 1 — narrow implement paths (operator control plane only).
  # Do not open whole packages; workspace control-plane paths stay out of scope.
  "packages/core/src/operator/**",
  "packages/core/src/flag/flag.ts",
  "packages/core/src/command.ts",
  "packages/core/src/config/experimental.ts",
  "packages/core/src/v1/config/config.ts",
  "packages/core/src/v1/config/migrate.ts",
  "packages/core/test/operator/**",
  "packages/core/test/command.test.ts",
  "packages/core/test/config/config.test.ts",
  "packages/opencode/src/operator/**",
  "packages/opencode/src/dev/sandbox/**",
  "packages/opencode/src/cli/cmd/op.ts",
  "packages/opencode/src/tool/registry.ts",
  "packages/opencode/src/mcp/catalog.ts",
  "packages/opencode/src/mcp/index.ts",
  "packages/opencode/test/operator/**",
  "packages/opencode/test/dev/sandbox/**",
  "packages/server/src/**/operator/**",
  "packages/cli/src/**/operator/**",
  "packages/tui/src/**/operator/**",
  "packages/tui/src/**/settings/**",
  "packages/schema/src/**/operator/**",
  "packages/schema/src/operator-event.ts",
  "packages/schema/src/durable-event-manifest.ts",
  "packages/schema/src/event-manifest.ts",
  "packages/schema/test/event-manifest.test.ts",
  "packages/core/src/event.ts",
  "packages/core/src/event/sql.ts",
  "packages/protocol/src/**/operator/**",
  "packages/sdk/**/operator/**",
  "packages/sdk-next/**/operator/**",
  "scripts/dev/opencode-operator-sandbox*",
  ".gitignore",

  # Feature 001 — Smart Agent Routing and Telemetry.
  # Existing paths (extended):
  "packages/core/src/observability/otlp.ts",
  "packages/opencode/src/session/todo.ts",
  "packages/opencode/src/agent/agent.ts",
  "packages/opencode/src/event-v2-bridge.ts",
  # New paths (novo):
  "packages/core/src/observability/telemetry-instruments.ts",
  "packages/opencode/src/routing/**",
  "packages/opencode/src/session/routing-state.ts",
  "packages/cli/src/**/smart/**",
  "packages/cli/src/**/routing/**",
  "packages/cli/src/**/telemetry/**",
  "packages/tui/src/**/smart/**",
  "packages/tui/src/**/routing-state/**",
  "packages/tui/src/component/prompt/index.tsx",
  "packages/tui/package.json",
  "packages/schema/src/**/routing/**",
  "packages/schema/src/**/telemetry/**",
  "packages/protocol/src/**/routing/**",
  "packages/protocol/src/**/telemetry/**",
  "doc/arch/sdd/001-define-one-cohesive-smart-agent-routing-and-opentelemetry/**",
  "doc/arch/adr/0001-opentelemetry-telemetry-foundation.md",
  "doc/arch/adr/0002-core-smart-agent-routing.md",

  # Repository-health remediation (speckit validate errors: readme, pin,
  # lint, ai-signature families). Narrow, file-exact globs — remove once
  # the corpus is green.
  "README.md",
  "package.json",
  ".npmrc",
  "tsconfig.json",
  "packages/containers/*/Dockerfile",
  "packages/opencode/Dockerfile",
  "packages/stats/server/Dockerfile",
  "packages/opencode/test/config/markdown.test.ts",
  "packages/script/package.json",
  "bun.lock",
  "packages/opencode/git",
  "packages/tui/src/component/prompt/cwd.ts",
  "packages/opencode/test/config/plugin.test.ts",

  # Repository-health: server listener robustness fix. startWithPortFallback
  # preferred port 4096 via a spurious loopback bind that succeeds under
  # SO_REUSEADDR even when a foreign process holds the 0.0.0.0:4096 wildcard,
  # so opencode claimed a port it did not exclusively own on loopback; and two
  # PTY route tests carried fixed poll deadlines that flaked under CPU
  # contention. Narrow, file-exact globs — remove once the corpus is green.
  # The pre-commit hook cleared inherited GIT_INDEX_FILE/GIT_DIR/GIT_WORK_TREE so
  # its isolated `git worktree add` validation no longer aborts on direct fcustom
  # commits (relative `.git/index` broke inside the new worktree).
  "packages/opencode/src/server/server.ts",
  "packages/opencode/test/server/httpapi-listen.test.ts",
  "packages/opencode/test/server/httpapi-v2-pty.test.ts",
  ".husky/pre-commit",

  # Feature 001 implement-phase paths (T001-T039): schema/protocol modules,
  # their test suites, CLI Spec-tree registration, and observability tests.
  "packages/schema/src/routing/**",
  "packages/schema/src/telemetry/**",
  "packages/schema/src/tui/**",
  "packages/schema/src/index.ts",
  "packages/schema/test/**",
  "packages/protocol/src/routing/**",
  "packages/protocol/src/telemetry/**",
  "packages/protocol/src/api.ts",
  "packages/protocol/test/**",
  "packages/cli/src/commands/**",
  "packages/cli/src/index.ts",
  "packages/core/test/observability/**",
  "packages/opencode/test/routing/**",
  "packages/opencode/test/telemetry/**",
  "packages/tui/test/**",

  # Feature 002 implement-phase paths (T001-T041): lifecycle schema/protocol
  # modules, the framework-free domain engine, application adapters, operator
  # command impls, CLI process/task commands, the TUI process panel, and the
  # lifecycle test suites. Narrow, file-exact globs.
  # Shared seams already in scope and not duplicated:
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001),
  #   packages/schema/src/durable-event-manifest.ts + packages/core/src/event.ts
  #     + packages/opencode/src/operator/** (Feature 007),
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001),
  #   packages/protocol/test/** + packages/tui/test/** (Feature 001).
  "packages/schema/src/lifecycle/**",
  "packages/protocol/src/lifecycle/**",
  "packages/core/src/lifecycle/**",
  "packages/opencode/src/lifecycle/**",
  "packages/cli/src/**/process/**",
  "packages/cli/src/**/task/**",
  "packages/tui/src/routes/session/process-panel/**",
  "packages/core/test/lifecycle/**",
  "packages/opencode/test/lifecycle/**",

  # Feature 003 implement-phase paths (T001-T036): jobs schema/protocol modules,
  # the framework-free domain scheduler engine, application adapters, CLI
  # op-jobs verbs, and the jobs test suites. Narrow, file-exact globs.
  # Shared seams already in scope and NOT duplicated:
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001) — publishJobEvent;
  #   packages/schema/src/durable-event-manifest.ts + packages/core/src/event.ts
  #     + packages/core/src/operator/** + packages/core/test/operator/**
  #     + packages/opencode/src/operator/** + packages/opencode/test/operator/**
  #     (Feature 007) — durable manifest, EventV2 authority, reserved jobs.*
  #     catalog bump (1.2.0 -> 1.3.0), and the operator/jobs command impls;
  #   packages/tui/src/**/operator/** (Feature 007) — jobs panel;
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001),
  #   packages/protocol/test/** (Feature 001) — schema barrel + schema/protocol tests.
  "packages/schema/src/jobs/**",
  "packages/protocol/src/jobs/**",
  "packages/core/src/jobs/**",
  "packages/opencode/src/jobs/**",
  "packages/cli/src/**/jobs/**",
  "packages/core/test/jobs/**",
  "packages/opencode/test/jobs/**",

  # Feature 004 implement-phase paths (T001-T042): langlock schema/protocol
  # modules, the framework-free domain policy/detection engine, application
  # enforcement seams, operator langlock.* command impls, CLI op-langlock verbs,
  # the TUI Lang Lock panel, and the langlock test suites. Narrow, file-exact
  # globs. Shared seams already in scope and NOT duplicated:
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001) — publishLangLockEvent;
  #   packages/opencode/src/agent/agent.ts (Feature 001) — reapply injection after transform;
  #   packages/schema/src/durable-event-manifest.ts + packages/core/src/event.ts
  #     + packages/core/src/operator/** + packages/core/test/operator/**
  #     + packages/opencode/src/operator/** + packages/opencode/test/operator/**
  #     (Feature 007) — durable manifest, EventV2 authority, reserved langlock.*
  #     catalog (already present at RESERVED_CATALOG_VERSION; no bump), and the
  #     operator/langlock command impls;
  #   packages/tui/src/**/operator/** + packages/tui/src/**/settings/** (Feature 007)
  #     — Lang Lock panel and Settings row;
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001),
  #   packages/protocol/test/** (Feature 001) — schema barrel + schema/protocol tests.
  "packages/schema/src/langlock/**",
  "packages/protocol/src/langlock/**",
  "packages/core/src/langlock/**",
  "packages/opencode/src/langlock/**",
  "packages/opencode/src/operator/langlock/**",
  "packages/cli/src/**/langlock/**",
  "packages/tui/src/**/operator/langlock/**",
  "packages/schema/test/langlock/**",
  "packages/protocol/test/langlock/**",
  "packages/core/test/langlock/**",
  "packages/opencode/test/langlock/**",
  # Existing shared seams extended (already in scope; listed for traceability):
  # "packages/opencode/src/session/llm/request.ts",   # reapply injection after transform
  # "packages/opencode/src/agent/agent.ts",           # reapply injection after transform
  # "packages/opencode/src/event-v2-bridge.ts",       # publishLangLockEvent

  # Feature 005 implement-phase paths (T001-T046): outputspool schema/protocol
  # modules, the framework-free domain spool engine, application adapters, the
  # operator output.* command impls, CLI op-output verbs, the TUI paging panel,
  # and the outputspool test suites. Narrow, file-exact globs.
  # Shared seams already in scope and NOT duplicated:
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001) — publishOutputEvent;
  #   packages/schema/src/durable-event-manifest.ts + packages/core/src/event.ts
  #     + packages/core/src/operator/** + packages/core/test/operator/**
  #     + packages/opencode/src/operator/** + packages/opencode/test/operator/**
  #     (Feature 007) — durable manifest, EventV2 authority, reserved output.*
  #     catalog (already at RESERVED_CATALOG_VERSION 1.3.0; no bump), and the
  #     operator/output command impls;
  #   packages/tui/src/**/operator/** (Feature 007) — output paging panel host;
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001),
  #   packages/protocol/test/** (Feature 001) — schema barrel + schema/protocol tests.
  "packages/schema/src/outputspool/**",
  "packages/protocol/src/outputspool/**",
  "packages/core/src/outputspool/**",
  "packages/opencode/src/outputspool/**",
  "packages/opencode/src/operator/outputspool/**",
  "packages/cli/src/**/output/**",
  "packages/tui/src/**/operator/output/**",
  "packages/schema/test/outputspool/**",
  "packages/protocol/test/outputspool/**",
  "packages/core/test/outputspool/**",
  "packages/opencode/test/outputspool/**",
  # C16 migration seams (real globs — not covered by any existing glob; T034 writes these):
  "packages/core/src/background-job.ts",
  "packages/core/src/tool-output-store.ts",

  # Feature 006 implement-phase paths (T001-T045): semantic schema/protocol
  # modules, the framework-free domain retrieval engine, application adapters
  # (Milvus client + embedding/rerank clients over the reused OpenAI-compatible
  # transport), the operator semantic.* command impls, CLI op-semantic verbs, the
  # TUI Semantic Search / Models panel, and the semantic test suites. Narrow,
  # file-exact globs. Shared seams already in scope and NOT duplicated:
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001) — publishSemanticEvent;
  #   packages/schema/src/durable-event-manifest.ts + packages/core/src/event.ts
  #     + packages/core/src/operator/** + packages/core/test/operator/**
  #     + packages/opencode/src/operator/** + packages/opencode/test/operator/**
  #     (Feature 007) — durable manifest, EventV2 authority, reserved semantic.*
  #     catalog (already at RESERVED_CATALOG_VERSION 1.3.0 with all 30 IDs; no bump),
  #     and the operator/semantic command impls;
  #   packages/tui/src/**/operator/** + packages/tui/src/**/settings/** (Feature 007)
  #     — Semantic Search / Models panel host and Settings row;
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001),
  #   packages/protocol/test/** (Feature 001) — schema barrel + schema/protocol tests;
  #   package.json + bun.lock (repository-health block above) — T025 adds the
  #     @zilliz/milvus2-sdk-node gRPC SDK there; no new glob required.
  "packages/schema/src/semantic/**",
  "packages/protocol/src/semantic/**",
  "packages/core/src/semantic/**",
  "packages/opencode/src/semantic/**",
  "packages/opencode/src/operator/semantic/**",
  "packages/cli/src/**/semantic/**",
  "packages/tui/src/**/operator/semantic/**",
  "packages/schema/test/semantic/**",
  "packages/protocol/test/semantic/**",
  "packages/core/test/semantic/**",
  "packages/opencode/test/semantic/**",

  # Feature 008 implement-phase paths (T001-T046): Complete MCP Client Tools and
  #   Resources Lifecycle. New mcp schema/protocol/core modules, the operator
  #   mcp.* domain impls, the TUI mcp panels, the four mcp test trees, and the
  #   existing migration seams reworked in place (C27). Feature 001-007 paths are
  #   preserved unchanged. Reused/shared seams already in scope are noted (not
  #   duplicated):
  #   packages/opencode/src/mcp/catalog.ts + .../mcp/index.ts (Feature 007 block
  #     above) are file-exact entries subsumed by the broader
  #     packages/opencode/src/mcp/** added here for the reworked service host
  #     (progress-sink/resource-adapter/spool-bridge/secret-bridge/experimental/**/
  #     reindex-trigger/logging-bridge/sdk-probe);
  #   packages/opencode/src/operator/** + packages/tui/src/**/operator/** (Feature
  #     007) already cover the operator/mcp and TUI mcp panel roots — the
  #     mcp-scoped globs below are listed explicitly per the 004/005/006 precedent;
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001) — publishMcpEvent;
  #   packages/schema/src/durable-event-manifest.ts + packages/core/src/event.ts
  #     + packages/opencode/src/operator/** + packages/opencode/test/operator/**
  #     (Feature 007) — durable manifest, EventV2 authority, reserved mcp.* catalog
  #     already at RESERVED_CATALOG_VERSION 1.3.0 (no bump, C25);
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001),
  #   packages/protocol/test/** (Feature 001) — schema barrel + schema/protocol tests;
  #   packages/opencode/src/cli/cmd/op.ts + packages/cli/src/**/operator/** (Feature
  #     007) — `opencode op mcp <op>` routes through op.ts and the operator registry,
  #     so no packages/cli/src/**/mcp/** glob is required (the reworked verbs live at
  #     packages/opencode/src/cli/cmd/mcp.ts, added below).
  "packages/schema/src/mcp/**",
  "packages/protocol/src/mcp/**",
  "packages/core/src/mcp/**",
  "packages/opencode/src/operator/mcp/**",
  "packages/tui/src/**/operator/mcp/**",
  "packages/schema/test/mcp/**",
  "packages/protocol/test/mcp/**",
  "packages/core/test/mcp/**",
  "packages/opencode/test/mcp/**",
  # Migration seams (existing files reworked in place by Feature 008):
  "packages/opencode/src/mcp/**",             # legacy MCP.Service reworked behind its seams (C27); broadens the file-exact catalog.ts + index.ts (Feature 007)
  "packages/schema/src/mcp-event.ts",         # mcp.tools.changed -> mcp.tools_changed rename (C3)
  "packages/opencode/src/cli/cmd/mcp.ts",     # opencode op mcp <op> verbs + legacy shim coexistence (FR48, C27)
  # Generated-artifact task (listed for traceability; follows the event rename):
  # "packages/sdk/js/src/v2/gen/types.gen.ts", # regenerate for mcp.tools_changed (T011)

  # Feature 009 implement-phase paths (T001-T016): Semantic Tool Search
  #   (embeddings + reranker extended to every tool-search surface). Feature 009
  #   is a strict EXTENSION of the implemented Feature 006 stack — only two paths
  #   are genuinely new (the C15 live-consumption seams). Everything else is
  #   ALREADY in scope and is NOT re-added (listed as comments for traceability):
  #   packages/schema/src/semantic/** (Feature 006) — ToolDoc, ToolSource,
  #     ToolDocId, tool-doc.ts, schema barrel additions;
  #   packages/protocol/src/semantic/** (Feature 006) — retrieveTools port,
  #     ToolRetrievalRequest collection discriminator, tool index payloads;
  #   packages/core/src/semantic/** (Feature 006) — tool-pass.ts, instruments ext;
  #   packages/opencode/src/semantic/** (Feature 006) — tool-projection,
  #     retrieval-facade ext, tool-reindex-trigger, tool-retrieval, index-jobs,
  #     cutover-executor, eval-harness ext;
  #   packages/{schema,protocol,core,opencode}/test/semantic/** (Feature 006) —
  #     all tool test suites;
  #   packages/opencode/src/tool/registry.ts (Feature 007) — describeCodeMode +
  #     tools() consumption seam;
  #   packages/core/src/config/experimental.ts (Feature 007) — per-surface flags;
  #   packages/core/src/v1/config/config.ts (Feature 007) — config threading;
  #   packages/opencode/src/mcp/** (Feature 008) — mcp.tools_changed trigger wiring;
  #   packages/schema/src/index.ts + packages/schema/test/** (Feature 001) —
  #     schema barrel registration + schema tests;
  #   packages/protocol/test/** (Feature 001) — protocol parity tests;
  #   doc/arch/schemas/semantic/*.cue (always-derived doc/arch/**) — tool-doc CUE
  #     mirrors (tool-doc.cue, tool-doc-parts.cue, ids-tool.cue, enums-tool.cue);
  #   the reserved semantic.* catalog at RESERVED_CATALOG_VERSION 1.3.0 is reused
  #     with NO new operator ID and NO catalog bump (FR22).
  # Genuinely new implement paths (the C15 live-consumption seams):
  "packages/opencode/src/session/tools.ts",   # native + MCP live-consumption seam (C15)
  "packages/opencode/src/tool/code-mode.ts",  # code-mode describeCatalog ranked-subset seam (C10, C15)

  # Feature 010 implement-phase paths (T001-T022): Native Rust FFI Tools and PTY
  #   Integration. The FIRST Rust in the repository — two cdylib crates + one
  #   shared rlib behind the existing TypeScript tool boundaries, plus the Bun-side
  #   loader/wrapper/registry/reader seam and the build pipeline. Native is
  #   authoritative for nothing; the permission gate stays in TS (Feature 007),
  #   output identity with Feature 005, telemetry content-free (ADR-0001).
  #   ALREADY IN SCOPE — listed for traceability only, NOT re-added:
  #     .gitignore                                  (Feature 007 block above) — /target + packages/core/native/*/ (C1, C6, C17)
  #     package.json                                (repository-health block above) — "build:native" script (C17)
  #     packages/core/src/config/experimental.ts    (Feature 007 block above) — experimental.nativeTools / nativePty booleans (C2)
  # Rust workspace (genuinely new to the repository):
  "Cargo.toml",
  "Cargo.lock",
  "rust-toolchain.toml",
  "crates/**",
  # TypeScript wrapper + fallback seam (genuinely new):
  "packages/core/src/tool/native/**",
  # Tool files touched only if the wrapper hooks into Tool.make (listed defensively):
  "packages/core/src/tool/bash.ts",             # opt-in pty:true route + permission-first ordering (C12, C20)
  "packages/core/src/tool/glob.ts",             # native backend selection before the Ripgrep.Service call (C5)
  "packages/core/src/tool/grep.ts",             # native backend selection before the Ripgrep.Service call (C5)
  "packages/core/src/tool/read.ts",             # wrapper hook (defensive)
  "packages/core/src/tool/read-filesystem.ts",  # wrapper hook (defensive)
  "packages/core/src/tool/write.ts",            # wrapper hook (defensive)
  "packages/core/src/tool/edit.ts",             # wrapper hook (defensive)
  "packages/core/src/tool/apply-patch.ts",      # wrapper hook (defensive)
  # Tests + build pipeline (genuinely new):
  "packages/core/test/tool/native/**",
  "packages/core/test/fixtures/native-parity/**",
  "script/build-native.ts",
  # Feature 023 — native tools default-on + compiled-binary dylib co-location.
  "packages/opencode/script/build.ts",  # ships the co-located dylibs into the compiled dist (FR-B)

  # Feature 011 implement-phase paths (T001-T017): Restructure the Operator
  #   Control Plane TUI from a flat list into grouped navigation with editable
  #   forms and honest availability. Presentation and navigation ONLY — no new
  #   dispatch path, no backend change, no new flag; the Feature 007
  #   OperatorClient loopback parity invariant (FR8) is preserved. Nearly the
  #   entire surface is ALREADY in scope under the Feature 007 / Feature 001
  #   blocks and is NOT re-added (listed as comments for traceability):
  #   packages/core/src/operator/** (Feature 007) — palette.ts label copy +
  #     availability/input-mode metadata + 12-domain OPERATOR_SETTINGS_DOMAINS +
  #     group-list/domain-panel projection builders; catalog.ts metadata read
  #     (mutates, 12 domains) with NO catalog change and NO version bump;
  #   packages/tui/src/**/operator/** (Feature 007) — dialog-settings.tsx home +
  #     domain-panel split, execute.ts unchanged dispatch path, the new reusable
  #     operator form under .../operator/form/**, and the five read panels
  #     .../operator/{jobs,output,langlock,semantic,mcp}/index.tsx;
  #   packages/tui/src/**/settings/** (Feature 007 / Feature 004) — the
  #     DialogLangLockPicker source pattern generalised by the form;
  #   packages/core/test/operator/** (Feature 007) — palette metadata/label unit
  #     tests;
  #   packages/tui/test/** (Feature 001) — dialog navigation, form dispatch,
  #     honest-unavailable, and parity tests (new packages/tui/test/operator/**
  #     lives under this glob).
  # Genuinely new implement path (the top-level palette host, not under any glob):
  "packages/tui/src/app.tsx",                 # collapse the flat listOperatorPaletteEntries spread → one Operator entry (FR1)

  # Feature 012 implement-phase paths: Expose the structured operator command
  #   result through the TUI. Consumption ONLY — forward the already-typed
  #   CommandResult.effective across the inbound adapter seam, thread it through
  #   the TUI context + dispatch into the five read panels and entity pickers, and
  #   add a Dialog push back-stack primitive. No new dispatch path, no backend
  #   behavior change, no new command id, no new flag; the Feature 007
  #   OperatorClient loopback parity invariant (FR9) is preserved. Nearly the
  #   entire surface is ALREADY in scope and is NOT re-added (traceability only):
  #   packages/opencode/src/operator/** (Feature 007) — tui-port.ts inbound adapter
  #     forwards outcome/effective/version on the SAME tryHandle return (FR1); it is
  #     the ONLY operator-backend file this feature touches;
  #   packages/tui/src/**/operator/** (Feature 007) — execute.ts return widening
  #     (FR3), dialog-settings.tsx DialogOperatorReadPanel signal feed (FR5), the
  #     form/ entity-picker loaders (FR6), and the five {jobs,langlock,output,
  #     semantic,mcp}/state.ts per-domain projection functions (FR4);
  #   packages/tui/test/** (Feature 001) — projection, loader, forward/thread,
  #     navigation, and parity tests (new packages/tui/test/operator/** is covered).
  # Genuinely new implement paths (not under any existing glob):
  "packages/tui/src/context/**",              # OperatorSlashHandled optional structured result (FR2)
  "packages/tui/src/ui/dialog.tsx",           # push(input, onClose?) back-stack primitive (FR7)

  # Feature 013 implement-phase paths: Wire the four remaining config-backed
  #   operator domains (telemetry, smart, budget, pools) from generic
  #   not_implemented stubs onto real typed domain ports over the already-live
  #   effective config, following the Feature 004 langlock domain-stack template.
  #   Feature 007 stays the sole command-registration authority — no catalog id is
  #   added and no catalog version is bumped (FR11); mutations persist through the
  #   SAME Config.Service seam langlock uses (FR7). Nearly the entire surface is
  #   ALREADY in scope and is NOT re-added (traceability only):
  #   packages/opencode/src/operator/** (Feature 007) — the four domain stacks
  #     (operator/{telemetry,smart,budget,pools}/{*-port,*-command-port,backend-live,
  #     persistence,stack-wiring,index}.ts), the stack-live.ts wireDomainPorts
  #     wiring (FR9), domain-stubs.ts stub removal, and config-status.ts
  #     STATUS_SHOW_IDS shadow removal (FR9);
  #   packages/core/src/operator/** (Feature 007) — palette.ts
  #     OPERATOR_PERSISTING_DOMAINS availability flip (FR12);
  #   packages/protocol/src/**/telemetry/** (Feature 001) — the telemetry protocol
  #     command module (FR1);
  #   packages/schema/src/telemetry/** + packages/schema/src/routing/** (Feature 001)
  #     — the reused effective-config shapes (telemetry config, RoutingConfig
  #     Activation/Enforcement.budget/Models.role_pools); reused, not re-authored;
  #   packages/opencode/test/operator/** (Feature 007), packages/protocol/test/**
  #     + packages/schema/test/** (Feature 001) — the per-domain unit, CAS-conflict,
  #     honest-unavailable, probe, and parity tests.
  # Genuinely new implement paths (protocol command modules not under any glob):
  "packages/protocol/src/smart/**",           # smart.* protocol command module (FR1, FR3)
  "packages/protocol/src/budget/**",          # budget.* protocol command module (FR1, FR4)
  "packages/protocol/src/pools/**",           # pools.* protocol command module (FR1, FR5)

  # Feature 014 implement-phase paths: Complete the operator control plane
  #   persistence and service. Close the two Feature 013 residuals — operator
  #   config mutations do not round-trip persist, and several service domains are
  #   still honest capability gaps. Accept the `operator` config namespace, align
  #   the Config.update write path with a loader-consumed read path, invalidate the
  #   config cache (FR1-FR4); convert the last two self-committing command ports
  #   (langlock, jobs) to the mutation_plan contract (FR5); wire the reachable
  #   service backends — OutputSpool control store (FR6), MCP admin host (FR7), the
  #   config-backed semantic registry (FR8) — while Milvus/executor/cancel edges
  #   stay typed capability gaps (FR9, FR10). Feature 007 stays the sole
  #   command-registration authority — no catalog id is added and no catalog
  #   version is bumped (FR13). Nearly the entire surface is ALREADY in scope and is
  #   NOT re-added (traceability only):
  #   packages/core/src/v1/config/config.ts (Feature 007 block) — ConfigV1.Info
  #     operator sub-schema (FR1);
  #   packages/opencode/src/operator/** (Feature 007) — langlock/jobs mutation_plan
  #     conversion + stack-live.ts service-backend wiring + lifecycle gap (FR5-FR11);
  #   packages/opencode/src/outputspool/** (Feature 005) — control store reused (FR6);
  #   packages/opencode/src/mcp/** (Feature 008) — MCP.Service host reused (FR7);
  #   packages/opencode/src/semantic/** (Feature 006) — registry/resolver reused (FR8);
  #   packages/opencode/src/jobs/** (Feature 002) — jobs persistence seam reused (FR9);
  #   packages/core/src/operator/** (Feature 007) — palette.ts Partial refinement (FR12);
  #   packages/{opencode/test/operator,core/test/operator}/** — commit/backend/
  #     availability/parity tests.
  # Genuinely new implement paths (config round-trip write/read alignment):
  "packages/opencode/src/config/**",          # operator-key accept + write/read align + cache invalidation (FR1-FR3)
  "packages/opencode/test/config/**",         # round-trip persistence tests (FR4)

  # Feature 017 implement-phase paths: Close the implementable operator
  #   capability gaps. Wire the gaps whose live dependency already exists — MCP
  #   live reads over MCP.Service status()/clients() (FR1-FR2), MCP mutations via
  #   the mutation_plan contract + live-service action plans (FR3-FR5), the
  #   OutputSpool production writer at the session message-part seam + stat/read/
  #   follow + the store-scoped admin edge (FR6-FR10), the jobs occurrence
  #   projection over EventV2Bridge (FR11-FR12), and the Milvus registry binding
  #   when an endpoint is configured (FR13-FR14) — plus the operator-TUI editing
  #   surface FIRST (multi-field edit modals + the detail view tree, FR19-FR23),
  #   the availability-map flip (FR15), and repo health (FR18). The executor
  #   (jobs.run-now), lifecycle forced-abort (cancel), and Smart Routing edges
  #   stay typed capability gaps (FR16). Feature 007 stays the sole
  #   command-registration authority — no catalog id, no catalog version bump, no
  #   new dispatch path (FR17). Nearly the entire backend surface is ALREADY in
  #   scope and is NOT re-added (traceability only):
  #   packages/opencode/src/operator/** (Feature 007) — mcp/output/jobs/semantic
  #     backend wiring + stack-live composition (FR1-FR14);
  #   packages/opencode/src/mcp/** (Feature 008) — MCP.Service host reused (FR1-FR5);
  #   packages/opencode/src/outputspool/** (Feature 005) — writer/reader/control
  #     store reused (FR6-FR10);
  #   packages/opencode/src/semantic/** (Feature 006) — milvus-adapter reused (FR13);
  #   packages/opencode/src/jobs/** (Feature 002/003) — occurrence events reused (FR11);
  #   packages/core/src/operator/** (Feature 007) — palette.ts availability flip (FR15);
  #   packages/{opencode/test/operator,core/test/operator,tui/test/operator}/** — tests.
  # Genuinely new implement paths (session writer hook + TUI editing + repo health):
  "packages/opencode/src/session/**",         # production spool writer at the message-part seam (FR6, FR10)
  "packages/opencode/.gitignore",             # git-ignore the runtime-generated config.json (FR18)
  "packages/tui/src/operator/form/**",        # multi-field edit modal + descriptor (FR19-FR22)
  "packages/tui/src/operator/status.ts",      # detail view tree renderer, distinct from the compact strip (FR23)

  # Feature 018 implement-phase paths: Compose the scheduled-jobs executor runtime
  #   into the live runtime. Arm the shipped-but-idle Feature 002/003 scheduler +
  #   cron + trigger service eagerly and fail-open at server start (FR1-FR3),
  #   implement the TaskProcessCoordinator over the real Feature 002 execution
  #   seams (FR4-FR6), convert jobs.run-now to an effectful mutation plan (FR7),
  #   emit definition-keyed occurrence events (FR8), expose the live
  #   SessionRunCoordinator through a narrow interrupt registry (FR9-FR10), and
  #   flip the palette availability to truth (FR11). Feature 007 stays the sole
  #   command-registration authority — no catalog id, no version bump, no new
  #   dispatch path or flag (FR12). Most of the surface is ALREADY in scope and is
  #   NOT re-added (traceability only):
  #   packages/opencode/src/jobs/** (Feature 003) — scheduler engine, cron adapter,
  #     trigger service, and the composition root + coordinator impl (FR1-FR8);
  #   packages/opencode/src/operator/** (Feature 007) — run-now conversion,
  #     occurrence-projection, and the lifecycle stack-wiring interrupt consult
  #     (FR7-FR10);
  #   packages/opencode/src/server/server.ts (repository-health block above) —
  #     the eager executor-composition bootstrap at listen() (FR1, FR2);
  #   packages/opencode/src/event-v2-bridge.ts (Feature 001) — definition-keyed
  #     job.* occurrence events (FR8);
  #   packages/opencode/src/session/** (Feature 017 block above) — the opencode
  #     session execution seam the coordinator runs through (FR5);
  #   packages/core/src/operator/** (Feature 007) — palette.ts availability flip (FR11);
  #   packages/{opencode/test/operator,opencode/test/jobs,core/test/jobs}/** — tests.
  # Genuinely new implement paths (narrow interrupt registry + TaskTool seam):
  "packages/core/src/session/interrupt-registry.ts",  # process-singleton narrow interrupt registry (FR9)
  "packages/core/src/session/run-coordinator.ts",     # narrow interrupt-key export for the registry (FR9)
  "packages/core/src/session/execution/**",           # execution layer registers the active root run at run start (FR9)
  "packages/opencode/src/tool/task.ts",               # TaskProcessCoordinator wires onto the TaskTool execution surface (FR4)
  "packages/core/test/session/**",                    # interrupt-registry + execution-hook tests (FR9)

  # Feature 037 — Wire the operator Smart Routing engine into the live session (Phase 1 MVP).
  #   Compose a session-local routing service over the session-layer Provider/Config/Agent/Auth
  #   interfaces and select an implicit default model at the session model-selection seam when
  #   Smart Routing is explicitly enabled in `auto` mode. Most of the surface is ALREADY in scope
  #   and is NOT re-added (traceability only):
  #   packages/opencode/src/session/** (Feature 017 block above) — the new routing-resolve.ts
  #     resolver and the prompt.ts model-selection seam edits;
  #   packages/opencode/src/routing/** + src/session/routing-state.ts (Feature 001) — the routing
  #     engine + RoutingSessionState decision-reference store this feature composes/consumes.
  # Genuinely new implement path (session-layer regression test):
  "packages/opencode/test/session/**",                # session-layer Smart Routing resolver tests (FR)

  # Feature 041 — Silence the MCP tool-schema unknown-format warning flood.
  #   Inject a custom MCP JSON-schema validator that recognizes the schemars
  #   unsigned-integer formats (uint/uint8/uint16/uint32/uint64) as no-op, so the
  #   SDK default Ajv no longer logs `unknown format "uintN"` once per tool-schema
  #   property on every MCP connect. The two touched source seams are ALREADY in
  #   scope and are NOT re-added (traceability only):
  #   packages/opencode/src/mcp/** (Feature 008 block) — the new schema-validator.ts
  #     factory and the createClient() wiring in mcp/index.ts;
  #   packages/opencode/test/mcp/** (Feature 008 block) — the validator unit test.
  # Genuinely new implement path (the direct ajv/ajv-formats dep the custom Ajv needs):
  "packages/opencode/package.json",                   # add ajv + ajv-formats as direct deps for the custom validator (FR)

  # Feature 040 — Bring the operator TUI to full parity with the op CLI. The routing.configure
  #   modal prefills from the EFFECTIVE, scope-resolved authority (ADR-0040 Option A): the
  #   config-status routing.status read resolves the layered effective config, the prefill
  #   extractor reads the Feature 036 nested activation shape, and the modal threads the Request
  #   scope + re-reads on switch. All source + test seams are ALREADY in scope and are NOT
  #   re-added (traceability only):
  #   packages/opencode/src/operator/** (Feature 007) — createRoutingStatusHandler effective read;
  #   packages/tui/src/operator/form/** (Feature 017 block) — the prefill extractor + scope thread;
  #   packages/{opencode/test/operator,tui/test/operator}/** — the parity + regression tests.
  # Genuinely new paths — the Feature 036 residual this feature DELIBERATELY supersedes (the
  #   project/bare routing.status `configured:false`-under-global residual now shadows):
  "doc/arch/adr/0036-fix-the-operator-routing-status-query-so-it-honors-the.md",
  "doc/arch/sdd/036-fix-the-operator-routing-status-query-so-it-honors-the/**",

  # Feature 046 — Expose the hierarchy/capability/budget enforcement config leaves
  #   through BOTH the op CLI and the TUI, driven by a single shared leaf registry.
  #   Most seams are ALREADY in scope and are NOT re-added (traceability only):
  #   packages/opencode/src/operator/** (Feature 007) — the new enforcement/ backend +
  #     command ports + budget extension + stack/authority wiring;
  #   packages/core/src/operator/** (Feature 007) — the catalog domains + entries;
  #   packages/tui/src/**/operator/** (Feature 017) — the generated field-list forms;
  #   packages/{opencode,core,tui}/test/** + packages/protocol/test/** (Feature 001/007)
  #     — the round-trip, scope, rejection, and op↔TUI parity tests.
  # Genuinely new path — the shared enforcement-leaf registry (protocol), the single
  #   source of truth both operator surfaces consume so they never drift (FR4):
  "packages/protocol/src/enforcement/**",

  # Feature 047 — Emit real OpenTelemetry spans + metrics for routing decisions,
  #   budget consumption, fan-out admission, and orchestration outcomes over the
  #   shipped process-singleton OTLP export pipeline. Most seams are ALREADY in
  #   scope and are NOT re-added (traceability only):
  #   packages/opencode/src/routing/** (Feature 001) — telemetry-export.ts domain-signal
  #     offer + the new application/telemetry-emitters.ts pure mappers;
  #   packages/opencode/src/session/** (Feature 017) — routing-resolve.ts / processor.ts /
  #     routing-hierarchy.ts emission wiring;
  #   packages/opencode/src/tool/task.ts (Feature 001 block) — the orchestration-outcome seam;
  #   packages/opencode/test/telemetry/** + test/session/** (Feature 001/017) — the emitter tests.
  # Genuinely new path — the pure attribute-mapping emitters module (listed for exactness):
  "packages/opencode/src/routing/application/telemetry-emitters.ts",

  # Feature 049 — Wire the hierarchy dispatch resolver into the live LLM `task`-tool
  #   execution path. Most seams are ALREADY in scope and are NOT re-added
  #   (traceability only):
  #   packages/opencode/src/session/** (Feature 017) — tools.ts ctx.extra injection,
  #     prompt.ts live-seam wiring + command-subtask model fix, routing-hierarchy.ts
  #     createLiveHierarchyResolve / toHierarchyDispatchExtra;
  #   packages/opencode/src/tool/task.ts (Feature 044 block) — per-invocation consume;
  #   packages/schema/src/**/routing (n/a) — SubtaskPart.model already optional;
  #   packages/opencode/test/session/** + test/routing/** — resolver seam tests.
  # Genuinely new path — the live-seam consumption test on the TaskTool surface:
  "packages/opencode/test/tool/task.test.ts",

  # Feature 050 — Wire the semantic index data plane (production pipeline runner,
  #   doc builders, skill chunker + OutputSpool, LiveDocSource, model-driven
  #   dimension discovery, data-plane retry). Most seams ALREADY in scope
  #   (traceability only):
  #   packages/{schema,core,opencode}/src/**/semantic/** + operator/semantic/**
  #     + cli/**/semantic/** + both test/semantic/** (Feature 006);
  #   packages/opencode/src/operator/** (Feature 007) — stack-live composition;
  #   packages/opencode/src/session/** (Features 017/031) — budget-consume
  #     retry_count activation;
  #   packages/core/src/config/experimental.ts (Feature 007) — latencyBudgetMs.
  # Genuinely new paths — shared transient-retry helper + models.dev metadata seam:
  "packages/opencode/src/util/effect-http-client.ts",
  "packages/core/src/models-dev.ts",
  "packages/core/src/util/token.ts",

  # Feature 051 — Wire live per-turn semantic narrowing of agents/skills/tools
  # into the three existing seams (`describeTask`, `SystemPrompt.skills`,
  # `session/tools.ts`) via the Feature 009 `ToolRetrieval.narrow`/`narrowRecord`
  # gate. Most seams ALREADY in scope (traceability only):
  #   packages/opencode/src/tool/registry.ts (Feature 007) — describeTask seam;
  #   packages/opencode/src/session/** (Feature 017) — system.ts skills seam,
  #     tools.ts native/MCP seam, routing-state.ts memo field;
  #   packages/opencode/src/semantic/** + test/semantic/** (Feature 006) —
  #     live-narrowing.ts orchestrator;
  #   packages/core/src/config/experimental.ts (Feature 007) — semantic_narrowing
  #     gate block;
  #   packages/opencode/test/session/** — system.ts/tools.ts seam tests.
  # Genuinely new path — the agents-seam pin/narrow test suite (no existing
  # `packages/opencode/test/tool/**` glob covers this file):
  "packages/opencode/test/tool/registry.test.ts",
  # Genuinely new path — the live-narrowing config resolver unit test (no
  # existing `packages/core/test/config/**` glob covers this file):
  "packages/core/test/config/experimental.test.ts",
  # Phase 4 (T019) — the composition root that mounts `SemanticRetrieval.Service`
  # into the live `AppLayer` and overrides the shipped degraded runner with the
  # Milvus/binding/embeddings/reranker seams. No existing glob covers this file.
  "packages/opencode/src/effect/app-runtime.ts",

  # Feature 052 — Auto skill semantic chunk auto-priming (SR-C), fourth
  # retrieval pass over `skill_chunks` + Tier-2 `<auto_skills>` render. Config +
  # provenance slice (FR5, the `#AutoSkillConfig` gate). Most seams ALREADY in
  # scope (traceability only):
  #   packages/{schema,core,opencode}/src/**/semantic/** + test/semantic/**
  #     (Feature 006) — narrowing-config.ts `AutoSkillConfig`, documents.ts
  #     `SkillProvenance`, skill-doc.ts provenance threading;
  #   packages/core/src/config/experimental.ts + v1/config/config.ts
  #     (Feature 007) — `skill_autoprime` gate block + `resolveAutoSkillConfig`;
  #   packages/core/test/config/experimental.test.ts (Feature 051) — resolver
  #     unit tests, extended.
  # Genuinely new paths — skill discovery provenance stamping (`Skill.Info`,
  # `isAutoprimable`) and the new per-pack `autoprime_urls` allowlist have no
  # existing glob:
  "packages/opencode/src/skill/**",
  "packages/opencode/test/skill/**",
  "packages/core/src/v1/config/skills.ts",

  # Feature 053 — Deterministic orchestration handoff: role-to-agent binding on
  #   `hierarchy.manager_agent`/`data_agent`/`composer_agent`, plus the always-on
  #   synchronous Data -> Composer interception before a manager-role spawn's prompt
  #   is finalized. Most seams ALREADY in scope and NOT re-added (traceability only):
  #   packages/schema/src/routing/config.ts (Feature 049 routing) — the three optional
  #     binding leaves;
  #   packages/protocol/src/enforcement/** (Feature 046 operator) — the hierarchy leaves
  #     for the `op hierarchy set/show` round-trip;
  #   packages/opencode/src/tool/task.ts (Feature 044/049) — binding resolution +
  #     interception call-site wiring;
  #   packages/opencode/src/session/** (Feature 017) — routing-state.ts synthetic flag,
  #     brief-validator.ts pure validator;
  #   packages/opencode/src/routing/application/telemetry-emitters.ts (Feature 047) —
  #     the content-free handoff emitter;
  #   packages/opencode/src/semantic/** (Feature 006/050) — retrieveAgents fresh catalog;
  #   packages/opencode/test/tool/task.test.ts (Feature 049) — call-site integration tests.
  # Genuinely new paths — the interception module + its unit test suite (no existing
  # `packages/opencode/{src,test}/tool/**` glob covers these files):
  "packages/opencode/src/tool/orchestration-handoff.ts",
  "packages/opencode/test/tool/orchestration-handoff.test.ts",
  # Residual (T022 live-smoke finding) — the server-route layer graph that serves
  # `opencode run`/TUI sessions builds its own `LayerNode.group` WITHOUT the
  # `SemanticRetrieval` mount, so every soft `serviceOption` seam (051 narrowing,
  # 052 auto-skill, 053 composer catalog retrieval) silently resolved absent in the
  # serving path. Mount-parity fix; no existing glob covers this file:
  "packages/opencode/src/server/routes/instance/httpapi/server.ts",

  # Feature 054 — Surface subagent provider/model/effort/tokens in the completed
  #   task line. Seams ALREADY in scope and NOT re-added (traceability only):
  #   packages/opencode/src/tool/task.ts + test/tool/task.test.ts (Feature
  #   044/049/053) — the spawn/completion metadata envelope stamps.
  # Genuinely new path — the TUI session route (renderer + colocated tests); no
  # existing glob covers the TUI package:
  "packages/tui/src/routes/session/**",
]

# Waivers for intentional near-empty files (warnings only; errors are never
# waivable). Each keeps a written justification per the waiver validator.

[[validate.waivers]]
rule = "hygiene.empty-file"
path = "packages/opencode/test/config/fixtures/no-frontmatter.md"
reason = "Deliberately minimal test fixture exercised by markdown.test.ts and skill.test.ts to cover the no-front-matter parsing path."
