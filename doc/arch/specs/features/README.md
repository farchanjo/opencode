# Features (Gherkin)

Executable Gherkin scenarios were removed from the scored corpus because `speckit verify` has no project step harness (all scenarios were unbound and zeroed `verifyHealth`).

Prose archive (git history + local): `.archive/feature-prose/`.

Executable contracts: package unit/integration tests + Acceptance Scenarios in each feature `spec.md`.
