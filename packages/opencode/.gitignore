research
dist
dist-*
gen
app.log
script/build-*.ts
temporary-*.md
.artifacts

# Runtime-generated by live CLI runs; carries the operator's real config plus
# plaintext secret material — never commit it (Feature 017 FR18).
config.json
